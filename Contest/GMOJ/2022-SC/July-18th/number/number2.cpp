#include <stdio.h>
int g[19][163][163];
int n;
void log_ten(long long x);
void Solve(int sum);
void Solve(int sum)
{
    int ed = log_ten(n);
    for (int i = ed; i >= 1; --i)
    {
        for (int s = 0; s <= sum; ++s)
        {
            for (int m = 0; m <= sum:
            for (int x = 0; x <= 9; ++x)
            {
                g[i][s + x][
